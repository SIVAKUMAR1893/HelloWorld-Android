pipeline {
    agent any
    
    stages {
        stage('Gradle') {
            steps {
                sh 'gradle --version'
            }
        }
        stage('Clean Build') {
               steps{
                    sh "pwd"
                    sh 'ls -al'
               
                    sh './gradlew clean'
               }
        }
    }
}
