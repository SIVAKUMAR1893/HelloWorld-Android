pipeline {
    agent any
    environment {
       PATH=$PATH:$GRADLE_HOME/bin

    }
    stages {
        stage('Gradle') {
            steps {
                sh 'gradle --version'
            }
        }
        stage('Clean Build') {
               steps{
                    sh "pwd"
                    sh 'ls -al'
                    sh 'gradle'
               
                    sh './gradlew clean'
               }
        }
    }
}
